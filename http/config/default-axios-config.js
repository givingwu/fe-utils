// import { Message } from 'element-ui'

export const DEFAULT_AXIOS_CONFIG = {
  timeout: 30 * 1000,
  // debug: process.env.NODE_ENV === 'development',
  /* showErrorMsg: function(title, message) {
    Message.error({
      message: message ? title + ':' + message : title,
      showClose: true,
    })
  }, */
}
